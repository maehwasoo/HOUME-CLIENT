/** 홈 배너 — home_banner_name, home_banner_id (DB 전송) */
export interface HomeBannerParams {
  home_banner_name?: string;
  home_banner_id?: number;
}

/** 다른 스타일로 시작 — home_style_name, home_style_id (DB 전송) */
export interface HomeStyleParams {
  home_style_name?: string;
  home_style_id?: number;
}
